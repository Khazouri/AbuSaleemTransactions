# خطة الاختبار — نظام أبو سليم للطلبات

دليل اختبار يدوي للنظام بأكمله، منظَّم **حسب الوظيفة**، ويغطي المسارات A–L (المراحل 1–84).

> النسخة الإنجليزية: [TEST_PLAN.md](TEST_PLAN.md) — المحتوى نفسه وعدد مربعات الاختيار نفسه.
> النسخة المنظَّمة حسب الدور: [TEST_PLAN.roles.ar.md](TEST_PLAN.roles.ar.md) / [.md](TEST_PLAN.roles.md).

**الفرق بين هذا الملف وخطة الأدوار.** ملف [TEST_PLAN.roles.ar.md](TEST_PLAN.roles.ar.md) يمشي في
النظام **قسمًا لكل دور** — يجيب على سؤال: "ماذا يستطيع R05 أن يفعل، وماذا يجب أن يُمنع منه؟". أما هذا
الملف فيمشي فيه **قسمًا لكل نظام فرعي** — ويجيب على سؤال: "هل تعمل دورة التظلم من أولها إلى آخرها؟".
الملفان يكملان بعضهما ولا يكرر أحدهما الآخر: استخدم هذا لإثبات أن الوظيفة تعمل، واستخدم خطة الأدوار
لإثبات أن مصفوفة الصلاحيات حقيقية. ولا يلغي أحدهما الآخر.

**لماذا اختبار يدوي أصلًا.** مجموعة الاختبارات الآلية تثبت كل مرحلة على حدة مقابل قاعدة sqlite في
الذاكرة، لكنها لا تثبت أن النظام يعمل لمن بُني من أجلهم. وهناك أصناف كاملة من الأخطاء لا تظهر إلا مع
MySQL ومتصفح حقيقي: ذهاب النص العربي وعودته، تخطيط RTL، بث الملفات الخاصة، الإشعارات المُدرَجة في
الطابور، وكل قاعدة يتوزع تطبيقها بين حارس Vue وطبقة الحماية في الـ API.

**كيف تستخدمه.** اعمل من الأعلى إلى الأسفل — الأقسام اللاحقة تفترض أن الطلب المُنشأ في §4/§5 ما زال
موجودًا. لكل خطوة مربع اختيار ونتيجة متوقعة صريحة. سجّل الإخفاقات في §16 بدل إصلاحها أثناء الجولة؛
فالنظام المُصلَح نصف إصلاح يُبطل كل الخطوات التي تليه.

**الحقائق المرجعية التي كُتبت هذه الخطة عليها** (أعِد اشتقاقها بالسكربت في §16 عند الشك):

| | |
|---|---|
| مراحل سير العمل | **12** |
| الأدوار | **11** (R01–R11) |
| الشاشات | **33** |
| حالات الطلب | **39** |
| أنواع أحداث الإشعار | **11** |
| مستويات الاعتماد | **5** |
| حسابات الاختبار المزروعة | **15** |

---

## 1. التهيئة

### 1.1 تشغيل النظام

```bash
# الواجهة الخلفية (جذر المستودع)
composer install
php artisan migrate            # يجب ألا يتبقى شيء معلق
php artisan db:seed            # البيانات المرجعية: الأدوار، الإدارات، المراحل، الشاشات، المصفوفة
php artisan db:seed --class=TestUserSeeder   # الحسابات الخمسة عشر أدناه

# التشغيل
composer dev                   # أو: php artisan serve / Homestead على http://abusaleem.test

# الواجهة الأمامية (من frontend/)
npm install
npm run dev                    # Vite على http://localhost:5173
```

- [ ] `php artisan migrate:status` يُظهر كل الهجرات بحالة `Ran` ولا شيء معلق.
- [ ] `curl http://abusaleem.test/api/ping` يعيد `{"message":"pong", ...}`.
- [ ] الواجهة تفتح على `http://localhost:5173` وتحوّل إلى `/login`.
- [ ] هناك عامل طابور يعمل (`php artisan queue:work`) — القيمة `QUEUE_CONNECTION=database`، ومن
      دونه **لا يُسلَّم أي إشعار في §12**، ويبدو الخلل كأنه عطل في المُرسِل لا كأنه عامل ناقص.

`TestUserSeeder` **لا يُستدعى** من `DatabaseSeeder` — شغّله صراحةً. وهذا مقصود: زرع الإنتاج يجب ألا
يُنشئ حسابات بكلمة مرور معروفة. والزارع idempotent، فإعادة تشغيله آمنة، لكنها **تُعيد ضبط** أي دور
غيّرته يدويًا.

منفذ خادم التطوير مثبَّت (`strictPort: true`). فإن كان 5173 مشغولًا، يفشل Vite **بصوت عالٍ** بدل أن
ينزلق إلى 5174 — وهو ما كان سيسلّم المتصفح أصلًا (origin) لا يسمح به `config/cors.php`، فتفشل كل
النداءات بوصفها "الخادم غير متاح" بينما الـ API سليم تمامًا.

- [ ] أوقف خادم التطوير، اشغل المنفذ 5173 بشيء آخر، ثم شغّل `npm run dev`: يرفض البدء برسالة
      "Port 5173 is already in use" بدل اختيار منفذ آخر بصمت.

### 1.2 إعادة الضبط بين الجولات

إعادة الضبط الكاملة هي
`php artisan migrate:fresh --seed && php artisan db:seed --class=TestUserSeeder`.
وهي تتلف كل الطلبات والاجتماعات والقرارات والتظلمات وصفوف التدقيق والنسخ الاحتياطية. ولإعادة ضبط
جزئية، تكفي إعادة تشغيل `TestUserSeeder` وحده لاستعادة الحسابات وأدوارها دون المساس ببيانات الطلبات.

> **لا تشغّل `db:seed --class=ScreenRolePermissionSeeder` على تثبيت عُدِّلت صلاحياته.** فهو يُعيد ضبط
> المصفوفة بأكملها إلى قيمها الافتراضية، ويُلغي كل صلاحية غيّرها مسؤول عبر شاشة الأدوار والصلاحيات.

### 1.3 حسابات الاختبار

كلمة المرور للجميع هي `password`، ولكل حساب رقم هاتف مضبوط (تحتاجه فحوص الرسائل القصيرة في §12 —
ولاحظ أن الهاتف **لا يمكن** ضبطه من شاشة المستخدمين، بل من هذا الزارع أو من شاشة تفضيلات الإشعارات
الخاصة بالمستخدم نفسه).

| # | البريد | الدور/الأدوار | الإدارة | الغرض |
|---|---|---|---|---|
| 1 | `r01.employee@abusaleem.test` | R01 | ENG | يقدّم الطلب. مديره المباشر هو #2. |
| 2 | `r02.reviewer@abusaleem.test` | R02 | REP | المقرر — القيد والبوابات والمحاضر والإقفال. |
| 3 | `r03.head@abusaleem.test` | R03 | CMT | رئيس اللجنة — يدعو ويقرر ويعتمد المحاضر. |
| 4 | `r04.member1@abusaleem.test` | R04 | CMT | يصوّت. |
| 5 | `r04.member2@abusaleem.test` | R04 | CMT | يصوّت. |
| 6 | `r04.member3@abusaleem.test` | R04 | CMT | يصوّت — ثلاثة أعضاء حتى تكون أغلبية 2-1 ممكنة. |
| 7 | `r05.manager@abusaleem.test` | R05 | ADM | مسار الموارد البشرية + مستوى الاعتماد 3. |
| 8 | `r06.ministry@abusaleem.test` | R06 | ABS | الوزارة — مستوى الاعتماد 4 والتصدير. |
| 9 | `r07.director@abusaleem.test` | R07 | ABS | الاعتماد النهائي — المستوى 5. |
| 10 | `r08.sysadmin@abusaleem.test` | R08 | ADM | مدير النظام. مدير ثانٍ حتى يمكن إيقاف أحدهما بأمان. |
| 11 | `multi.role@abusaleem.test` | R03+R04 | CMT | يثبت أن `screenPermissions()` **تُوحِّد** بين الأدوار. |
| 12 | `inactive.user@abusaleem.test` | R01 | FIN | `is_active = false` — يُرفض عند الدخول **وكفاعل**. |
| 13 | `r09.secretary@abusaleem.test` | R09 | CMT | أمين سر اللجنة — إعداد جدول الأعمال. |
| 14 | `r10.diwan@abusaleem.test` | R10 | ABS | مسار الديوان عند `receive_and_register`. |
| 15 | `r11.legal@abusaleem.test` | R11 | CMT | العضو القانوني — الدور الوحيد الذي يسجّل مراجعة قانونية. |

- [ ] تظهر الحسابات الخمسة عشر في شاشة المستخدمين بحساب R08، ويظهر #12 موقوفًا.
- [ ] بالدخول بحساب #1، شاشة المستخدمين ليست في القائمة الجانبية و`/users` مرفوض.

### 1.4 منتقي حسابات الاختبار في شاشة الدخول

المنتقي محكوم من **الواجهة الخلفية** لا من البناء: `GET /api/dev/test-users` يرد 404 ما لم يكن
`APP_ENV=local`. لذا فإن نفس مجلد `dist/` يُظهر المنتقي أمام API محلي ويخفيه أمام API حقيقي، ولا
يُدمج أي عنوان بكلمة مرور معروفة داخل حزمة الواجهة إطلاقًا.

- [ ] مع `APP_ENV=local` تظهر لوحة المنتقي في شاشة الدخول، ونقرة واحدة تُدخلك.
- [ ] `curl http://abusaleem.test/api/dev/test-users` يعيد الحسابات الخمسة عشر، ولا تحتوي الاستجابة
      على **أي تجزئة كلمة مرور**.
- [ ] اضبط `APP_ENV=production` وامسح ذاكرة الإعدادات وأعد التحميل: تختفي اللوحة ويرد المسار 404
      (لا 403 — فالـ 403 يؤكد وجود المسار).

---

## 2. الصلاحيات والتنقل

جدول `screen_role_permissions` هو المصدر الوحيد للحقيقة، وتقرأه **كلتا** الطبقتين: حماية الـ API
(`screen.permission:<code>,<action>`) وحارس التوجيه في Vue. والخاصية المختبَرة هنا هي أنهما لا
يختلفان أبدًا.

### 2.1 القائمة الجانبية لكل دور

ادخل بكل حساب واحسب عناصر القائمة. الـ API يعيد شاشات أكثر مما تعرضه القائمة، لأن `request_details`
و`notes_attachments` يحملان `:id` في مسارهما فتُستبعدان من القائمة — أي أن **القائمة = الـ API − 2**
لكل دور.

| الدور | الشاشات عبر الـ API | عناصر القائمة |
|---|---|---|
| R01 | 21 | 19 |
| R02 | 22 | 20 |
| R03 | 22 | 20 |
| R04 | 21 | 19 |
| R05 | 22 | 20 |
| R06 | 22 | 20 |
| R07 | 21 | 19 |
| R08 | **33** | 31 |
| R09 | 20 | 18 |
| R10 | 20 | 18 |
| R11 | 20 | 18 |

- [ ] عدد عناصر القائمة لكل دور يطابق الجدول.
- [ ] R08 وحده يرى كتلة الإدارة (المستخدمون، الإدارات، الأدوار والصلاحيات، الإعدادات، القوالب، النسخ
      الاحتياطي، الصيانة).
- [ ] مجموعة **الاجتماعات** تظهر كقسم قابل للطي يضم ثماني شاشات، بينما تقع `decisions` **خارجه**
      كعنصر مستقل في المستوى الأعلى.
- [ ] `multi.role@` يرى **اتحاد** شاشات R03 وR04 لا تقاطعهما. وهو الحساب الوحيد الذي يكشف هذا
      التراجع لو حدث.

### 2.2 اتفاق الحارس مع الـ API

- [ ] بحساب R01 اكتب `/users` في شريط العنوان: يمنع الحارس الانتقال وتصل إلى وجهة معقولة لا إلى شاشة
      فارغة.
- [ ] بحساب R01 نادِ `GET /api/users` برمزك: **403**، لا 200 ولا 404.
- [ ] بحساب R08 افتح الأدوار والصلاحيات، اسحب `requests → view` من R01 واحفظ. ادخل بحساب R01: اختفت
      شاشة الطلبات من القائمة ورفضها الـ API. أعِد الصلاحية بعدها.

### 2.3 العرض والتصدير والاعتماد صلاحيات مختلفة

- [ ] بحساب R01 افتح التقارير: تُعرض، ولا يوجد **أي** زر تصدير.
- [ ] بحساب R01 نادِ `GET /api/reports/requests/export?format=xlsx`: **403**.
- [ ] بحساب R06 يعيد النداء نفسه ملف `.xlsx`.
- [ ] بحساب R06 ينجح أيضًا `GET /api/decisions/export` (أضافت المرحلة 84 هذه الفئة — كانت محصورة
      بـ R08 على غير اتساق مع بقية عمليات التصدير).
- [ ] بحساب R04: `GET /api/decisions/export` يرد **403**.

### 2.4 الحذف حكر على R08

- [ ] بحساب R05 لا تُظهر شاشة الإدارات أي أداة حذف، ويرد `DELETE /api/departments/{id}` بـ 403.
- [ ] بحساب R08، حذف إدارة لها أبناء أو مستخدمون **مرفوض** مع بيان السبب — البيانات المرجعية تُعطَّل
      (`toggle-active`) ولا تُمحى.

---

## 3. المصادقة والجلسة

- [ ] بيانات الدخول الصحيحة تعيد رمزًا، وتحفظه الواجهة وتنتقل إلى لوحة التحكم.
- [ ] كلمة المرور الخاطئة تعيد رسالة عامة **لا تكشف** ما إذا كان البريد موجودًا.
- [ ] `inactive.user@` يُرفض برسالة خاصة به.
- [ ] سبع محاولات دخول متتالية تصطدم بحد `throttle:6,1` وتعيد **429**. انتظر دقيقة قبل المتابعة —
      فهذا أكثر سبب شائع لظهور "رمز فارغ" محيّر أثناء جولة يدوية.
- [ ] إعادة تحميل الصفحة تُبقيك داخل النظام (الرمز في `localStorage`).
- [ ] ألغِ الرمز من جهة الخادم ثم نفّذ إجراءً في الواجهة: يلتقط معترض 401 الأمر ويخرجك إلى `/login`
      بدل ترك شاشة معطلة.
- [ ] سجّل الخروج: الرمز يختفي، وزر الرجوع لا يستعيد شاشة الداخل.

---

## 4. الاستلام والترقيم ونقطة القيد

هنا تعمل قاعدة [D] المادة 15: تسليم الطلب إلى الرئيس المباشر **ليس قيدًا**. الموظف يحصل على
*إيصال*، والرقم الإشاري يُمنح لاحقًا.

- [ ] بحساب R01 افتح شاشة استلام الطلب. اختر نوعًا — تظهر **قائمة المستندات المطلوبة** مقسومة إلى
      "الأساسية المشتركة" و"الخاصة بالنوع"، مع تعليم البنود المشروطة.
- [ ] أرفق ملف PDF وصورة PNG؛ كل رفع يستوجب تصنيف الملف في أحد مجلدات الملحق 14. لا قيمة افتراضية —
      والرفع غير المصنَّف مرفوض.
- [ ] أرسل الطلب. تعرض شاشة النجاح **إيصال استلام** بالصيغة `PM-RCV/2026/000001` وتذكر صراحةً أن هذا
      إيصال لا قيد.
- [ ] قيمة `reference_number` للطلب الجديد **فارغة**، ومرحلته `direct_manager_review` — إذ ينتقل
      الاستلام إليها تلقائيًا في المعاملة نفسها.
- [ ] حاول تقديم طلب **ثانٍ** من النوع نفسه للموظف نفسه: يُرفض مع تسمية الملف المفتوح لإلحاق
      المستندات به (قاعدة عدم الازدواجية في الملحق 16).
- [ ] قدّم طلبًا من نوع *مختلف*: يُقبل. وهذا ما تسمح به القاعدة فعلًا.

---

## 5. المسار الكامل — اثنتا عشرة مرحلة

امشِ بطلب واحد من أوله إلى آخره. والمقصود أن **لا حساب واحد يستطيع إتمامه**؛ والاضطرار إلى R08
لتجاوز أي خطوة هو بذاته خلل.

| # | المرحلة | الفاعل | الإجراء |
|---|---|---|---|
| 1 | `receive_from_municipality` | R01 | `submit` (تلقائي عند الاستلام) |
| 2 | `direct_manager_review` | #2 (المدير المباشر لمقدّم الطلب) | `forward` |
| 3 | `administrative_routing` | المدير نفسه | `route_to_hr` — أنهت المرحلة ٩٦ مساري الديوان وأمين السر |
| 4 | `receive_and_register` | R12 (الموارد البشرية) | `register` |
| 5 | `requirements_check` | R02 | البوابات ثم `approve` — **وهذا هو القيد** |
| 6–8 | `reviewer_review` · `observations` · `forward_to_committee` | — | خارج المسار منذ المرحلة ١٠٢: `approve` في المرحلة 5 ينقل الملف إلى المرحلة 9، قائمة الطلبات المعلقة للجنة |
| 9 | `receive_from_committee` | R03 | تصويت ثم تسجيل القرار |
| 10 | `approval_by_authority` | R05 | `approve` |
| 11 | `local_governance_ministry` | R06 | `approve` |
| 12 | `final_approval_archiving` | R07 | `approve` ← `in_execution` |

- [ ] الخطوة 2 تعمل بحساب **#2 تحديدًا** (مدير R01 المعيَّن) لا بأي حساب R02 آخر.
- [ ] حساب R02 آخر ليس مديرًا لهذا الموظف يُرفض في الخطوة 2.
- [ ] الخطوة 3: اختر **الديوان**. ثم حاول `register` بحساب R05 (مسجّل الموارد البشرية): **مرفوض** —
      فبوابة الحالة حقيقية لا شكلية.
- [ ] `register` بحساب R10 ينجح، وتصير الحالة `in_review` **لا** `registered`.
- [ ] الخطوة 5: حاول `approve` قبل تسجيل اختبار الاختصاص (المادة 45): **مرفوض**.
- [ ] سجّل اختبار الاختصاص (بأسئلته الستة) وبوابة الاستلام (§6)، ثم `approve`. **الآن** يُمنح الرقم
      الإشاري — `PM-COM/2026/0001` — وتصير الحالة `registered`.
- [ ] أعِد الملف بـ `return_missing_docs` ثم امشِ به عبر القفزة نفسها مرة أخرى: الرقم الإشاري **لم
      يتغير**. رقم واحد طوال الحياة (المادة 99).
- [ ] كل قفزة تكتب صفًا في سجل المراحل وصفًا في سجل الحالات، ويعرض الخط الزمني في شاشة الطلب الجهة
      والمستند المرتبط بكل إدخال.
- [ ] الخطوات 5 و9 و10 و11 و12 يتطلب كل منها **توقيعًا** ويكتب صفًا في سجل الاعتمادات — خمسة مستويات،
      واحد لكل نقطة تفتيش.
- [ ] تخطي مستوى مستحيل: بحساب R07 حاول الاعتماد والطلب ما زال في المرحلة 10. مرفوض.
- [ ] لا يعتمد الفاعل طلبه **هو**. اجعل #10 (R08) يقدّم طلبًا ثم يحاول اعتماده: مرفوض في كل مستوى.

### 5.1 مسار تجاوز الوزارة

- [ ] قدّم طلبًا نوعه ذو `decision_grade_threshold` أعلى من درجته. عند المرحلة 10 يقفز الاعتماد به
      **مباشرة إلى المرحلة 12** متجاوزًا الوزارة.
- [ ] حالته عند الوصول `final_approved` لا `approved` — فمسار التجاوز يختم الحالة الصحيحة، وهو الخلل
      الذي اضطرت المرحلة 57 إلى إصلاحه صراحةً.
- [ ] الطلب الذي يبلغ العتبة أو يتجاوزها يمر بالمرحلة 11 أولًا.

---

## 6. بوابات الرقابة الأربع

يوجب الملحق 63 أن تمنع المنظومة نفسها الانتقال عند أربع نقاط.

**البوابة 1 — قبل القيد.** عند `requirements_check`:

- [ ] كل مستند مطلوب مزروع يجب أن يُجاب عنه؛ وترك أحدها بلا إجابة يمنع `approve`.
- [ ] المستند الذي يصفه المصدر بأنه مشروط ("بحسب الموضوع") يجوز أن يُجاب عنه بـ **لا ينطبق**.
- [ ] وغير المشروط **لا يجوز**؛ وإسقاطه مرفوض مع تسميته.
- [ ] إقرار صحة الوقائع (الملحق 20) مطلوب.
- [ ] **بحساب R01 على طلبه هو:** تسجيل اختبار الاختصاص أو بوابة الاستلام **مرفوض** (المرحلة 84 —
      قاعدة الفصل في الملحق 19). فالموظف لا يشهد باكتمال ملفه بنفسه.
- [ ] وحتى حساب R02 الذي *أنشأ* الطلب نيابةً عن غيره يُرفض برسالة الملحق 19. أرسل حمولة **صحيحة**
      عند اختبار هذا — فالحمولة الخاطئة تفشل في التحقق أولًا فتستنتج خطأً أن المنع غير موجود.
- [ ] يعرض نصفا البوابة **من سجّلهما ومتى**.

**البوابة 2 — قبل جدول الأعمال.** مغطاة في §8.3.

**البوابة 3 — قبل إحالة المحضر للاعتماد.** مغطاة في §8.6.

**البوابة 4 — قبل الإقفال.** مغطاة في §10.2.

---

## 7. الاستثناءات والمدد والتصعيد

- [ ] كل من `return_missing_docs` و`reject_review` و`request_edit` و`cancel` **يستوجب سببًا**؛
      والإرسال بلا سبب مرفوض، ويظهر السبب في الخط الزمني.
- [ ] تظهر إجراءات الاستثناء كأزرار ثانوية بصريًا لا كبدائل مكافئة للإجراء الطبيعي.
- [ ] الطلب الملغى أو المؤرشف لا يقبل أي إجراء لاحق.
- [ ] اجعل `due_date` لطلب في الماضي وشغّل `php artisan requests:flag-overdue`: يُوسم **مرة واحدة**.
      أعِد التشغيل — لا شيء يتغير ولا يُطلَق إشعار ثانٍ.
- [ ] تعرض قائمة الطلبات نقطة التزام لكل مرحلة، ومستوياتها الأربعة هي مستويات الملحق 38: **أخضر**
      ضمن المدة، **أصفر** *قرب تجاوزها* (وما زال داخلها)، **أحمر** بعد تجاوزها، و**حرج** فقط عند وجود
      مدة قانونية مسجّلة.
- [ ] شغّل `php artisan requests:escalate-delays` على ملف تجاوز مدة مرحلته: يُشعَر **صاحب الإجراء
      الحالي**. زد التأخير: R02 + R05. سجّل `legal_deadline` عليه وأعد التشغيل: يصير حرجًا ويبلغ
      R03 + R07 — مع استمرار إبلاغ الدرجات الأدنى.
- [ ] أعِد التشغيل فورًا: **لا يُعلَن المستوى نفسه مرتين**.
- [ ] انقل الطلب إلى مرحلة أخرى: يُعاد ضبط السلّم ويمكن للمرحلة الجديدة أن تصعّد من جديد.

---

## 8. اللجنة والاجتماعات

### 8.1 اللجان

- [ ] بحساب R08 أنشئ لجنة وأضف أعضاءها بمقاعدهم (رئيس / قانوني / مدير موارد بشرية / مندوب وزارة /
      مقرر). ولا يُملأ المقعد الواحد مرتين.
- [ ] اللجنة التي لم تُسجَّل قاعدة نصابها تعرض **غير مثبت** لا رقمًا. فالنظام يرفض اختراع نصاب
      (الملحق 64 يمنع ذلك).
- [ ] سجّل "أكثر من نصف الأعضاء" على لجنة من أربعة أعضاء: النصاب المطلوب **3** لا 2. وهذا هو الفحص
      الذي يثبت أن `ceil(n/2)` المخترعة أُزيلت ولم يُعَد تسميتها فقط.
- [ ] حذف لجنة لها اجتماعات مرفوض.

### 8.2 المرشحات والمراجعة القانونية

- [ ] تعرض شاشة الطلبات المرشحة الملفات عند `receive_from_committee` بانتظار الترشيح، مع مدة
      الانتظار واكتمال الملف والاجتماع المقترح.
- [ ] بحساب R02 أرسل ملفًا إلى المراجعة القانونية. تصير حالته `under_legal_review`، و**لا تتحرك
      مرحلته**.
- [ ] بحساب R11 يظهر الملف في قائمة المراجعة القانونية ويُفتح بنجاح — وR11 لا يملك أي انتقال في سير
      العمل، فهذا يثبت أن توسيع الرؤية يعمل.
- [ ] بحساب R11 سجّل نتيجة. `sound_ready` و`present_with_note` يسمحان بجدول الأعمال، والثلاث الأخرى
      تمنع وتعيد الملف. والنتيجة المانعة تستوجب ملاحظة.
- [ ] بحساب R02، محاولة *تسجيل* نتيجة: 403. وبحساب R11، محاولة *إحالة* ملف إلى المراجعة: 403.
      فالصلاحيتان غير متبادلتين.

### 8.3 جدول الأعمال (البوابة 2)

- [ ] إدراج طلب لم تُراجَع قانونيته في جدول الأعمال **مرفوض**.
- [ ] الطلب الذي عليه تعارض مستندات لم يُعالَج مرفوض، بنص الملحق نفسه.
- [ ] تُعاد ترتيب البنود **بالسحب والإفلات**، ويبقى الترتيب محفوظًا.
- [ ] تحسب لوحة الترتيب رتبة المادة 83 (المؤجلة ← المرتبطة بمدة قانونية ← العاجلة ← المكتملة بحسب
      تاريخ جاهزيتها ← غير المصنفة). وزر "تطبيق الترتيب" يعيد كتابة الجدول.
- [ ] خالِف الترتيب المحسوب دون مبرر: تُبلِّغ الجاهزية بـ `agenda_order_departs_from_rule`
      **وتُمنع الدعوة للاجتماع** حتى تسجّل مبررًا.
- [ ] للأولوية مستويان اثنان فقط (عالية / عادية). والعالية المعلنة تستوجب سببًا من أسباب الملحق 33
      الخمسة **ومبررًا مكتوبًا**.
- [ ] أضف بندًا **إداريًا** (بلا طلب مرتبط): يستوجب موضوعًا، ولا يمكن أن يحمل قرارًا أبدًا.
- [ ] بحساب R04 حاول إضافة بند: **403** (المرحلة 84 — المادة 13 تمنح الأعضاء الدراسة والمناقشة
      والتصويت لا التحرير).
- [ ] بحساب R02 أضف بندًا: ينجح.

### 8.4 الجاهزية والدعوة للانعقاد

- [ ] تعرض شاشة الجاهزية أربع نسب والنصاب وقائمة **بالاستثناءات فقط**.
- [ ] الاجتماع الذي فيه ملف ناقص أو عضو غير مؤكد أو دعوة معلقة يُبلِّغ بالاستثناء المطابق و**ليس
      جاهزًا**.
- [ ] الدعوة لاجتماع جاهز لا تستوجب سببًا. أما الدعوة لاجتماع **غير** جاهز فتستوجب مبرر تجاوز مكتوبًا
      وتخزّنه.
- [ ] بحساب R02 جدوِل اجتماعًا للجنة **لست عضوًا فيها**: مرفوض برسالة "لا يجوز جدولة اجتماع للجنة لست
      عضوًا فيها." ثم جدوِل اجتماعًا للجنة أنت عضو فيها: ينجح.
- [ ] بحساب R04 جدوِل اجتماعًا: **403**.
- [ ] الدعوة للانعقاد تُجمِّد قواعد التصويت على الاجتماع. عدّل نصاب اللجنة بعدها: يظل الاجتماع المنعقد
      يُبلِّغ بالقواعد التي انعقد بموجبها.

### 8.5 إدارة الجلسة والتصويت والقرارات

- [ ] تعرض شاشة المباشرة البند الحالي ومؤقتًا وقائمة الحاضرين وتدفق المناقشة.
- [ ] بطاقة الدراسة ذات الخطوات التسع (المادة 85) تمنع التأشير خارج الترتيب — وتأشير خطوة قبل سابقتها
      مرفوض مع تسمية الخطوة الناقصة.
- [ ] خطوتان **مشتقتان لا تُؤشَّران**: التصويت وإثبات النتيجة.
- [ ] "عرض الرأي القانوني" يكون غير منطبق على بند لا رأي قانوني له.
- [ ] **التصويت مرفوض حتى يكتمل التسلسل** — وفي اللحظة نفسها تعرض قائمة "بانتظار تصويتي" لدى العضو
      **صفر صفوف**. ويجب أن تتفق الاثنتان.
- [ ] أكمل التسلسل: ينجح التصويت وتمتلئ القائمة.
- [ ] غير العضو لا يصوّت. والعضو المؤشَّر غائبًا لا يصوّت. والعضو الذي أعلن تعارض مصالح لا يصوّت
      **ولا** يكتب في تدفق المناقشة.
- [ ] بمجرد وجود صوت على بند، يُرفض رفع مرفق أو إعادة توليد مذكرة العرض لذلك البند (تجميد المادة في
      الملحق 25) — بينما ينجح **الرفع نفسه** على ملف آخر لم يُصوَّت عليه.
- [ ] سجّل قرارًا بأغلبية 2-1: ينجح. أما **التعادل** فمرفوض لا يُخمَّن، وكذلك التصويت الصفري.
- [ ] كل قرار يستوجب أجزاء الملحق 27 الأربعة (الموضوع / الوقائع / السند / المنطوق) وأداة المادة 90
      (قرار / توصية / رأي).
- [ ] منطوق مكوَّن من "اتخاذ اللازم" وحدها مرفوض، بينما يُقبل "اتخاذ اللازم نحو إحالة الملف … خلال
      أسبوع". فالقاعدة تختبر **عدم الكفاية** لا المنع.
- [ ] التأجيل يستوجب حقول المادة 34 الأربعة الإلزامية؛ و"تأجيل للمراجعة" المجردة مرفوضة.
- [ ] الرفض يستوجب **رمز سبب** مع تفصيل؛ و"لعدم الاستحقاق" المجردة مرفوضة.
- [ ] بحساب R02 (المقرر) يُرفض الإدلاء بصوت: المادة 16 تمنعه.

### 8.6 المحاضر (البوابة 3)

- [ ] ولّد المحضر: يجمع الحضور وقاعدة النصاب المطبقة وأصوات كل بند وقراره.
- [ ] **غيّر جدول الأعمال بعد التوليد ثم حاول الاعتماد:** مرفوض. وهذه هي القيمة الحقيقية للبوابة 3 —
      محضر معتمد يصف جلسة لم تقع.
- [ ] أعِد التوليد ثم اعتمد: ينجح، وتُسجَّل فحوص الملحق 8 الستة عشر.
- [ ] الاجتماع الذي **لم يُسجَّل فيه حضور** لا يُعتمد محضره — فهو لا يثبت حضورًا ولا صحة انعقاد.
- [ ] الاعتماد يُنشئ صف توقيع لكل حاضر مؤشَّر، وآخر توقيع يحوّل الوثيقة إلى `approved` تلقائيًا.
- [ ] من ليس موقِّعًا لا يوقّع، ولا يوقّع أحد مرتين.
- [ ] لا يُقفل الاجتماع قبل اعتماد محضره.
- [ ] بحساب R02، اعتماد المحضر: **403** — فهذا فعل الرئيس (المادة 12).

---

## 9. التظلمات

- [ ] بحساب R01 قدّم تظلمًا على طلب **مقرَّر** تملكه. والتظلم على طلب غير مقرَّر أو على طلب غيرك
      مرفوض.
- [ ] التظلم الثاني على الملف نفسه يستوجب **إقرارًا بوقائع جديدة**.
- [ ] أرفق مستندًا بالتظلم — يُحفظ على التظلم لا على الطلب الأصلي.
- [ ] بحساب R02 نفّذ التحقق الشكلي: القبول يمضي به، والرفض يستوجب سببًا ويقفله بحالة `rejected`.
- [ ] المتظلم لا يتحقق شكليًا من تظلمه بنفسه.
- [ ] سجّل اختبار الاختصاص: إجابة "اللجنة" تمضي إلى `file_assembly`، وأي من الإجابات الأربع الأخرى
      تُنهيه بحالة `outside_jurisdiction`.
- [ ] سجّل المراجعة القانونية (المادة 75) — أسئلتها الخمسة مطلوبة معًا.
- [ ] افتح **الملف** المجمَّع: يعرض الطلب الأصلي ومذكرة عرضه ومقتطف المحضر والقرار ومستندات التظلم
      نفسه وإثبات الإشعار داخل النظام. أما البريد والرسائل القصيرة فتُبلِّغ بصدق أنه *لا دليل متاح*
      بدل ادعاء التسليم.
- [ ] رشّح التظلم على جدول أعمال لجنة — ولا يمكن ذلك إلا بعد بلوغه `legal_review`.
- [ ] اتخذ فيه إحدى النتائج الخمس. وكل نتيجة تستوجب تعليقًا.
- [ ] نفّذ النتيجة: `appeal_accept` و`appeal_partial_accept` يجعلان الطلب **الأصلي** منتهيًا،
      و`appeal_reject` يتركه كما هو، و`appeal_refer` غير منتهٍ، و`appeal_redo` يعيد فتح الأصل عند
      مرحلة تسمّيها.
- [ ] أقفل التظلم: تُسجَّل بطاقة الإقفال ويُشعَر المتظلم.
- [ ] **لا يُقفل الطلب الأصلي ما دام عليه تظلم مفتوح.**
- [ ] أعِد فتح تظلم مقفل: لا يجوز إلا لسبب من الأسباب الستة المعدَّدة. ومجرد "لا أوافق على النتيجة"
      مرفوض.

---

## 10. التنفيذ والإقفال والإيقاف وإعادة الفتح

### 10.1 إثبات التنفيذ

- [ ] ادعاء التنفيذ **بلا مرفق دليل** مرفوض: "لا يكفي أن تقول الجهة المنفذة (تم التنفيذ)؛ يجب إرفاق
      دليل التنفيذ."
- [ ] ترشيح مرفق يخص طلبًا **آخر**: مرفوض.
- [ ] فحوص المنفِّذ الستة في النموذج 17: "لا" واحدة تَرفض مع اقتباس السؤال المخفق.
- [ ] على طلب موسوم بأثر مالي، يجب أن يكون فحص الإحالة "نعم" — و`not_applicable` مرفوضة (المادة 97).
- [ ] الفحص السابع (هل تم إرفاق مستند التنفيذ؟) **مشتق** — زوّر قيمته "لا" في الحمولة فتعود "نعم" من
      الحالة الحقيقية.
- [ ] قائمة سلامة القرار ذات الاثنتي عشرة نقطة (المادة 103) مطلوبة قبل `in_execution`. وثمانٍ منها
      مشتقة: زوّر `minutes_signed: "no"` فيُستبدل من الحالة الحقيقية.

### 10.2 الإقفال (البوابة 4)

- [ ] المسارات الثلاثة القابلة للإقفال في المادة 37 تعمل جميعًا: المنفَّذ، وغير الموافق عليه، وعدم
      الاختصاص.
- [ ] شروط الملحق 48 يَرفض كل منها **باسمه**: الملف المؤجل يقول "لا يجوز إقفال معاملة مؤجلة."،
      والمعاد من جهة الاعتماد يقول ذلك بعبارته، وهكذا.
- [ ] تدقيق الملحق 47 ذو الاثنتي عشرة نقطة: "لا" واحدة تَرفض مع اقتباس السؤال.
- [ ] اثنتان من الاثنتي عشرة **مشتقتان من الخادم** ولا تُسألان — فحص مسار التظلم وموقع الأرشفة.
- [ ] `final_result_code` و`notice_status` محسوبان: زوّرهما في الحمولة فتعود القيم الحقيقية.
- [ ] الإقفال مرتين مرفوض.
- [ ] بحساب R04، الإقفال: **403**. وبحساب R02 أو R03: ينجح.

### 10.3 الإيقاف وإعادة الفتح

- [ ] أوقف ملفًا بموجب المادة 105: تصير الحالة `execution_suspended`، و**لا تتحرك المرحلة**، ولا
      يُكتب صف في سجل المراحل.
- [ ] أثناء الإيقاف يُرفض `approve` عبر **كلٍّ** من مسار الانتقال وقائمة الاعتماد، و**لا يُكتب أي صف
      اعتماد** من المحاولة المرفوضة.
- [ ] الرفع مرفوض حتى توجد مراجعة قانونية مسجَّلة *بعد* الإيقاف.
- [ ] يظهر الملف الموقوف في قائمة المراجعة القانونية لدى R11.
- [ ] ارفع الإيقاف: `fact_confirmed` يعيد الحالة المجمَّدة، و`referred_to_committee` يعيده إلى
      `receive_from_committee`.
- [ ] أعِد فتح طلب منتهٍ لسبب من الأسباب الستة المعدَّدة: ينتقل إلى المرحلة التي تسمّيها، و**تُمسح
      سجلات بوابة الاستلام واختبار الاختصاص وسلامة القرار** جميعًا حتى لا تمر الجولة الجديدة بإجابات
      الجولة السابقة.
- [ ] سجل الإعادة من جهة الاعتماد **لا يُمسح** — فالجولات السابقة تبقى مقروءة.

---

## 11. السجلات والتقارير ومؤشرات الأداء

- [ ] تعرض شاشة السجلات سجلات المادة 98 **الاثني عشر** بالترتيب، ولكل منها أعمدته.
- [ ] السجل 2 (الناقصة) يُبقي صف ملف عولج نقصه **لاحقًا** — فسجل نواقص تختفي صفوفه عند الإصلاح هو
      قائمة عمل لا سجل. ويقرأ الصف `still_incomplete: لا`.
- [ ] السجل 6 يحمل أعمدة الملحق 12 **الثلاثة عشر**.
- [ ] السجل 7 يسمّي طلبه (الرقم والموضوع ممتلئان لا فارغان).
- [ ] السجل 11 يحمل حقول التأجيل الخمسة في المادة 34.
- [ ] رمز سجل غير معروف يعيد 404.
- [ ] التقارير: بطاقات المؤشرات وأشرطة التوزيع ومنحنى الاثني عشر شهرًا كلها تُعرض، والمرشّحات
      تُضيّقها.
- [ ] تبويب المادة 106 يعيد **اثني عشر** مؤشرًا بترتيب المادة نفسها، ولكل منها غرضه المذكور.
- [ ] المؤشر الذي لا شيء لديه ليقيسه يعيد **قيمة فارغة** لا صفرًا واثقًا.
- [ ] بطاقة زمن الطلب (T1–T10) تطابق متوسطات البلدية شريحةً بشريحة — فالاشتقاق نفسه يغذّي الاثنين.
- [ ] الشريحة التي لم يقع طرفاها كلاهما تعيد قيمة فارغة لا صفرًا.
- [ ] لوحة الاجتماعات تعرض خانات الملحق 11 الإحدى عشرة، وخانة "المتأخرة" **تتقاطع عمدًا** مع غيرها،
      وتذكر الاستجابة ذلك صراحةً.
- [ ] تنبيهات الملحق 10 يسمّي كل منها **الجهة التي ينتظرها الملف** لا مجرد عدد الأيام.
- [ ] صدّر سجلًا وتقريرًا بصيغتي `xlsx` و`pdf`. افتح ملف الـ PDF: **العربية مُشكَّلة وموصولة بشكل
      صحيح** لا حروفًا منفصلة من اليسار إلى اليمين.

---

## 12. الإشعارات

يتطلب عامل طابور يعمل (§1.1).

- [ ] تعرض شاشة التفضيلات **أحد عشر** نوع حدث في `NotificationSetting::EVENT_TYPES`، ولكل منها
      مفاتيح داخل النظام / البريد / الرسائل القصيرة.
- [ ] تقديم طلب خطوة يُشعِر المُنشئ **والفاعل التالي**، على القنوات المفعَّلة لديهما فقط.
- [ ] اكتم حدثًا بالكامل: لا يُسلَّم شيء على أي قناة.
- [ ] امسح هاتف مستخدم: تُسقَط قناة الرسائل القصيرة بدل إدراجها في الطابور بلا إمكان تسليم.
- [ ] يتحدّث شارة الجرس خلال دقيقة، وتسرد القائمة المنسدلة غير المقروء.
- [ ] **المادة 101:** يُشعَر الموظف عند كل لحظة مرتبطة. والحالة `registered` نفسها تُقرأ لحظةً أولى في
      المرة الأولى ولحظةً ثالثة بعد دورة نواقص.
- [ ] صف حالة قيمته from == to (مرحلتان متجاورتان تتشاركان حالة عامة) **لا يُشعِر أحدًا** — فهو
      سيعلن حالة لم يغادرها الملف.
- [ ] لا يُشعَر الفاعل بفعله هو أبدًا.
- [ ] **المادة 102:** حصيلة تصويت اللجنة تبلغ الأعضاء **لا** الموظف. وإشعار الموظف يقتبس "للأسباب
      المثبتة في القرار المعتمد" ولا يحوي أي حصيلة.
- [ ] لحظتا "المطلوب منك إجراء" وحدهما تقتبسان السبب المسجَّل، وتحجبه البقية.
- [ ] تعرض شاشة الطلب سجل الإشعارات التي أُرسلت فعلًا عن ذلك الملف.

---

## 13. سجل التدقيق

- [ ] إنشاء طلب وتعديله يكتبان صفوف تدقيق مع الفاعل، ويسجّل صف التعديل **الحقول المتغيّرة فقط** قديمها
      وجديدها.
- [ ] الحفظ بلا تغيير لا يكتب شيئًا.
- [ ] حقول كلمة المرور مُخفاة في الطرفين.
- [ ] `migrate:fresh --seed` **لا يكتب** أي صف تدقيق — فبيانات التهيئة ليست نشاط مستخدم.
- [ ] العارض يرشّح بالمستخدم والإجراء والنموذج والمدى الزمني.
- [ ] اسم صنف خام يرسله العميل مرفوض؛ فالمرشّح يتكلم بمفاتيح سجل قصيرة.
- [ ] بحساب R06 صدّر سجل التدقيق. وبحساب R01: 403.

---

## 14. الإدارة والنسخ الاحتياطي والصيانة

- [ ] الإدارات والمستخدمون والأدوار والصلاحيات والإعدادات والقوالب تعمل جميعًا بحساب R08.
- [ ] الإدارة التي لها أبناء أو مستخدمون لا تُحذف — تُعطَّل فقط.
- [ ] إسناد مدير لمستخدم يعمل من شاشة المستخدمين ويبقى بعد إعادة التحميل. (كان هذا شكليًا لفترة:
      الحقل في النموذج ولم يكن في عقد الـ API.)
- [ ] خذ نسخة احتياطية من شاشة النسخ. فإن لم يكن `mysqldump` على المسار، تُسجَّل المحاولة **فاشلة مع
      سببها على الشاشة** لا ضائعة في خطأ 500.
- [ ] نزّل نسخة؛ واحذف أخرى؛ والاحتفاظ لا يحذف القديم إلا بعد وجود نسخة أحدث.
- [ ] لا يوجد زر استعادة — فالاستعادة عملية على الخادم، والشاشة تقول ذلك.
- [ ] تعرض شاشة الصيانة (لـ R08 وحده) التشخيص: الهجرات المعلقة، وتوفر الصدفة مع سببه،
      و`max_execution_time`، والمسارات القابلة للكتابة، ونتيجة فحص كل ملف تنفيذي.
- [ ] يعرض كل أمر **سطر تنفيذه الحرفي** قبل تشغيله.
- [ ] أي نص خارج القائمة المعتمدة (`rm -rf /`، `migrate; whoami`) مرفوض **ولا يُسجَّل له تشغيل**.
- [ ] `migrate:fresh` و`migrate:rollback` يستوجبان صلاحية `approve` **مع** كتابة عبارة التأكيد التي
      يمليها الـ API.
- [ ] أمر artisan يعمل داخل العملية ويسجّل مخرجاته الحقيقية؛ وأمر الصدفة يُرفض بسبب واضح عندما يمنع
      المضيف `proc_open`.
- [ ] تشغيل ثانٍ متزامن يُرفض صراحةً بدل إدراجه في طابور.

---

## 15. التعريب والاتجاه والسمة والطباعة

- [ ] بدّل إلى العربية: يُضبط `dir="rtl"` على `<html>` وينعكس التخطيط بالكامل — القائمة الجانبية
      والجداول وعناوين الحقول والأيقونات.
- [ ] لا تنزلق أي شاشة أفقيًا في أي اتجاه عند عرض نافذة معتاد.
- [ ] كل نص ظاهر مترجَم؛ ولا تتسرب مفاتيح لغة خام (`meetings.foo.bar`).
- [ ] النص العربي المُدخَل في نموذج يذهب ويعود صحيحًا عبر الحفظ وإعادة التحميل — بلا `????`.
- [ ] بدّل السمة إلى الداكنة: تبقى كل شاشة مقروءة. وافحص تحديدًا أشرطة لوحة التحكم وشارات إجراءات سجل
      التدقيق وأقراص نتائج القرارات — فالألوان المكتوبة يدويًا تختبئ هناك.
- [ ] اضبط نظام التشغيل على الداكن والتطبيق على "تلقائي": يتبعه، بلا ومضة فاتحة في أول رسم.
- [ ] صورة التوقيع المخزَّنة تبقى مقروءة في السمة الداكنة (فهي حبر داكن على أبيض، وخلفيتها غير
      مُسمَّاة عمدًا).
- [ ] اطبع سجل القرارات ومقالًا من الدليل: تنبسط السمة الداكنة إلى حبر على أبيض، وتُطبع الصفحة كاملة
      لا أول شاشة منها فقط.

---

## 16. الانحدار والاعتماد

أعِد اشتقاق الأعداد بدل الوثوق بهذا المستند:

```bash
# المراحل / الأدوار / الشاشات / الحالات
php artisan tinker --execute="echo App\Models\WorkflowStage::count().' '.App\Models\Role::count().' '.App\Models\Screen::count().' '.App\Models\RequestStatus::count();"
# تطابق عدد مربعات الاختيار بين هذا الملف وتوأمه الإنجليزي
grep -c '^- \[ \]' TEST_PLAN.md TEST_PLAN.ar.md
```

- [ ] الأعداد تطابق جدول §0 (12 / 11 / 33 / 39).
- [ ] `vendor/bin/phpunit` أخضر.
- [ ] `vendor/bin/pint --test` لا يُبلِّغ بفروق في الملفات التي مسستها.
- [ ] `npm run build` ينجح.
- [ ] ملفا خطة الاختبار متساويان في عدد مربعات الاختيار.

### سجل الأخطاء

| # | القسم | ما حدث | المتوقع | الخطورة |
|---|---|---|---|---|
| | | | | |

### الاعتماد

| الدور | الحساب | نفّذها | التاريخ | النتيجة |
|---|---|---|---|---|
| R01 | `r01.employee@` | | | |
| R02 | `r02.reviewer@` | | | |
| R03 | `r03.head@` | | | |
| R04 | `r04.member1@` | | | |
| R05 | `r05.manager@` | | | |
| R06 | `r06.ministry@` | | | |
| R07 | `r07.director@` | | | |
| R08 | `r08.sysadmin@` | | | |
| R09 | `r09.secretary@` | | | |
| R10 | `r10.diwan@` | | | |
| R11 | `r11.legal@` | | | |

---

## ملحق — البيانات المرجعية

**المراحل الاثنتا عشرة بالترتيب:** `receive_from_municipality` · `direct_manager_review` ·
`administrative_routing` · `receive_and_register` · `requirements_check` · `reviewer_review` ·
`observations` · `forward_to_committee` · `receive_from_committee` · `approval_by_authority` ·
`local_governance_ministry` · `final_approval_archiving`.

**مستويات الاعتماد الخمسة:** `requirements_check` (1) · `receive_from_committee` (2) ·
`approval_by_authority` (3) · `local_governance_ministry` (4) · `final_approval_archiving` (5).

**سلاسل الترقيم (الملحق 15 / النموذج 05):** `PM-RCV/YYYY/000001` إيصال الاستلام ·
`PM-COM/YYYY/0001` الطلب · `PM-MTG/YYYY/01` الاجتماع · `PM-MIN/YYYY/01` المحضر ·
`PM-DEC/YYYY/001` القرار.

**الأدوار الأحد عشر:** R01 موظف · R02 المقرر · R03 رئيس اللجنة · R04 عضو اللجنة ·
R05 مدير إدارة الشؤون الإدارية · R06 وزارة الحكم المحلي · R07 المدير العام / العميد ·
R08 مدير النظام · R09 أمين سر اللجنة · R10 وكيل الديوان · R11 العضو القانوني.

**شاشات الاعتماد الخمس:** `reviewer_approval` · `committee_head_approval` ·
`admin_manager_approval` · `ministry_approval` · `final_approval`. (كانت ستًا حتى حذفت المرحلة 57
شاشة `authority_approval` مع مرحلة `competent_authority`.)
