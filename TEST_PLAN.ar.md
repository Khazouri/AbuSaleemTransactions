# خطة الاختبار — نظام معاملات أبو سليم

دليل اختبار يدوي شامل للنظام، من المرحلة 1 إلى المرحلة 27.

> النسخة الإنجليزية: [TEST_PLAN.md](TEST_PLAN.md) — المحتوى نفسه.

**لماذا هذا المستند موجود.** مجموعة الاختبارات الآلية (80 اختباراً / 538 تحققاً) تُثبت صحة كل
مرحلة على حدة مقابل قاعدة بيانات sqlite في الذاكرة. لكنها **لا** تُثبت أن النظام يعمل للأشخاص
الذين بُني من أجلهم، لأن الحساب الوحيد الذي كان موجوداً حتى الآن هو `admin@abusaleem.test` —
مدير النظام، الذي يملك الإجراءات السبعة كلها على الشاشات الثلاث والعشرين كلها. التصفح بهذا
الحساب يُظهر شاشات الاعتماد الست — المصممة لتكون لكل منها دور واحد فقط — دفعةً واحدة، وهو
عكس ما تنصّ عليه مصفوفة الصلاحيات تماماً. هذه الخطة تسير في النظام بالحسابات الاثني عشر التي
ينشئها `TestUserSeeder`، فتُختبر بوابات الأدوار فعلياً.

**كيف تستخدمها.** اعمل من الأعلى إلى الأسفل. لكل خطوة مربع تأشير ونتيجة متوقعة صريحة. سجّل
الإخفاقات في القسم 16 بدل إصلاحها أثناء الجولة — فنظام مُصلَح جزئياً يُبطل كل خطوة تليه.

---

## 1. التهيئة

### 1.1 تشغيل النظام

```bash
# الواجهة الخلفية (جذر المستودع)
composer install
php artisan migrate            # يجب ألّا يبقى أي ترحيل معلّق
php artisan db:seed            # البيانات المرجعية: الأدوار، الإدارات، المراحل، الشاشات، المصفوفة
php artisan db:seed --class=TestUserSeeder   # الحسابات الاثنا عشر أدناه

# التشغيل
composer dev                   # أو: php artisan serve / Homestead على http://abusaleem.test

# الواجهة الأمامية (من مجلد frontend/)
npm install
npm run dev                    # Vite على http://localhost:5173
```

- [ ] `php artisan migrate:status` يُظهر كل ترحيل بحالة `Ran` ولا شيء معلّق.
- [ ] `curl http://abusaleem.test/api/ping` يُرجع `{"message":"pong", ...}`.
- [ ] التطبيق يفتح على `http://localhost:5173` ويُحوّل إلى `/login`.

`TestUserSeeder` **غير** مُستدعى من `DatabaseSeeder` ويجب تشغيله صراحةً. هذا مقصود: تشغيل
`db:seed` على بيئة إنتاج يجب ألّا يُنشئ اثني عشر حساباً كلمة مرورها معروفة. البذرة قابلة
لإعادة التشغيل بأمان، وإعادة تشغيلها **تُعيد ضبط** أي دور غيّرته يدوياً.

### 1.2 إعادة الضبط بين الجولات

إعادة الضبط الكاملة هي
`php artisan migrate:fresh --seed && php artisan db:seed --class=TestUserSeeder`.
انتبه: هذا يمسح كل المعاملات والاجتماعات والقرارات وسجلات التدقيق والنسخ الاحتياطية. لإعادة
ضبط جزئية، تشغيل `TestUserSeeder` وحده يُعيد الحسابات وأدوارها دون المساس ببيانات المعاملات.

### 1.3 حسابات الاختبار

كلمة المرور للجميع هي `password`. لكل حساب رقم هاتف (لازم لاختبارات الرسائل النصية في القسم
11) — ولاحظ أن رقم الهاتف **لا يمكن** ضبطه من شاشة المستخدمين، بل من هذه البذرة فقط.

| # | البريد الإلكتروني | الدور | الإدارة | الغرض |
|---|---|---|---|---|
| 1 | `r01.employee@abusaleem.test` | R01 موظف | ENG | يُنشئ المعاملات. **ولا يُحرّك أياً منها** — R01 لا يظهر إطلاقاً في `workflow_transitions.required_role_id` |
| 2 | `r02.reviewer@abusaleem.test` | R02 المقرر | REP | المراحل 1→5، ويملك كل المسارات الاستثنائية الراجعة |
| 3 | `r03.head@abusaleem.test` | R03 رئيس اللجنة | CMT | المرحلة 7، ويُسجّل القرارات المُلزِمة |
| 4 | `r04.member1@abusaleem.test` | R04 عضو اللجنة | CMT | مُصوِّت |
| 5 | `r04.member2@abusaleem.test` | R04 عضو اللجنة | CMT | مُصوِّت |
| 6 | `r04.member3@abusaleem.test` | R04 عضو اللجنة | CMT | مُصوِّت — المقعد الثالث يجعل أغلبية 2-1 ممكنة |
| 7 | `r05.manager@abusaleem.test` | R05 مدير الشؤون الإدارية | ADM | المراحل 5 و6، ونقطة التفرّع في المرحلة 8 |
| 8 | `r06.ministry@abusaleem.test` | R06 وزارة الحكم المحلي | ABS | المرحلة 9، ويملك صلاحية التصدير للتقارير والتدقيق |
| 9 | `r07.director@abusaleem.test` | R07 المدير العام / العميد | ABS | المرحلتان 10 و11، **ولا يملك صلاحية الاستلام** |
| 10 | `r08.sysadmin@abusaleem.test` | R08 مدير النظام | ADM | شاشات الإدارة، مع إبقاء `admin@abusaleem.test` سليماً |
| 11 | `multi.role@abusaleem.test` | R03 **+** R04 | CMT | الصلاحيات يجب أن تكون **اتحاداً** لا تقاطعاً |
| 12 | `inactive.user@abusaleem.test` | R01 (`is_active=false`) | FIN | يجب رفض دخوله |

الحساب الموجود مسبقاً: `admin@abusaleem.test` / `password` (R08). اتركه دون تعديل ليبقى لديك
حساب إداري صالح إذا أوقف أحد الاختبارات الحساب رقم 10.

---

## 2. مصفوفة الصلاحيات

حارس المسارات في Vue (`frontend/src/router/index.js` ← `auth.can(meta.screenCode,'view')`)
موصوف صراحةً بأنه **لتحسين تجربة الاستخدام فقط**؛ الفرض الحقيقي يقع على
`CheckScreenPermission` في الواجهة البرمجية. الغرض الكامل من هذا القسم هو إثبات أن الاثنين
يتفقان دائماً.

### 2.1 القائمة الجانبية لكل دور

سجّل الدخول بكل حساب وقارن القائمة الجانبية بهذا الجدول. `transaction_details` و
`notes_attachments` تُرجعهما `GET /api/screens` لكن `AppSidebar.vue` يستبعدهما من القائمة
(لأنهما يحتاجان معرّف معاملة)، لذا فالعدد **الظاهر** أقل باثنتين من عدد الواجهة البرمجية أدناه.

| الحساب | عدد الشاشات في الواجهة البرمجية | شاشات الاعتماد الظاهرة |
|---|---|---|
| R01 موظف | 11 | *لا شيء* |
| R02 مقرر | 12 | `reviewer_approval` فقط |
| R03 رئيس لجنة | 12 | `committee_head_approval` فقط |
| R04 عضو لجنة | 11 | *لا شيء* |
| R05 مدير إدارة | 12 | `admin_manager_approval` فقط |
| R06 وزارة | 12 | `ministry_approval` فقط |
| R07 مدير عام | 12 | `authority_approval` **و** `final_approval` |
| R08 مدير نظام | 23 | الست جميعاً |
| multi.role (R03+R04) | 12 | `committee_head_approval` فقط |

- [ ] كل دور يرى شاشة اعتماد واحدة بالضبط (R07 اثنتان، R01/R04 لا شيء، R08 الست).
- [ ] **R07 لا يملك مدخل «استلام المعاملة» إطلاقاً.** هذا مقصود — المدير العام يعتمد ولا
      يُدخل البيانات.
- [ ] R01–R07 لا يرون `users` ولا `departments` ولا `roles_permissions` ولا `settings` ولا
      `templates` ولا `backup`. هذه الشاشات الست لمدير النظام حصراً.
- [ ] `multi.role` يرى **اتحاد** صلاحيات R03 وR04 — تحديداً يستطيع التصويت
      (`decisions,add` لـ R03+R04) **و** تسجيل القرار (`decisions,approve` لـ R03 وحده).

### 2.2 اتفاق حارس الواجهة مع الواجهة البرمجية

لكل شاشة ممنوعة، نفّذ الشقّين معاً:

- [ ] بحساب R01، اكتب `/users` في شريط العنوان ← يُحوَّل إلى `/dashboard` دون أن تظهر شاشة
      المستخدمين ولو للحظة.
- [ ] بحساب R01، `GET /api/users` ← **403** مع الرسالة
      `لا تملك صلاحية الوصول إلى هذا القسم.`
- [ ] بحساب R02، اكتب `/approvals/ministry` ← يُحوَّل إلى `/dashboard`.
- [ ] بحساب R02، `GET /api/approvals/ministry` ← 403.
- [ ] بحساب R04، `POST /api/meetings/{id}/agenda/{item}/decision` ← 403، مع أن R04 يستطيع
      التصويت على البند نفسه.

### 2.3 «العرض» و«التصدير» صلاحيتان مختلفتان

خمس شاشات تفصل بينهما عمداً. من يستطيع قراءة الشاشة يجب أن يُمنع من الحصول على الملف.

- [ ] R01: `GET /api/reports/transactions` ← 200، بينما
      `GET /api/reports/transactions/export?format=xlsx` ← **403**.
- [ ] R01: `GET /api/audit-logs` ← 200، بينما `GET /api/audit-logs/export?format=xlsx` ← **403**.
- [ ] R06 وR07 ينجحان في الحالتين أعلاه (يملكان صلاحية `export`).
- [ ] R06: `GET /api/decisions` ← 200، بينما `GET /api/decisions/export` ← **403**. تصدير
      القرارات **لمدير النظام حصراً** — البذرة تمنح `print` للجميع ولا تمنح `export` لأحد
      سواه. هذا التباين يسهل ظنّه خللاً، وهو ليس كذلك.
- [ ] R08: `GET /api/backups` ← 200 و`GET /api/backups/{id}/download` ← 200؛ وكل دور آخر
      يحصل على 403 في الحالتين.

### 2.4 لا أحد غير R08 يستطيع الحذف

`can_delete` مُعطّلة لـ R01–R07 على **كل** الشاشات.

- [ ] بحساب R03 (وهو يستطيع إنشاء اللجان)، `DELETE /api/committees/{id}` ← 403.
- [ ] بحساب R08، النداء نفسه ينجح (مع مراعاة حارس السجلّ في القسم 10.1).

---

## 3. المصادقة والجلسة

- [ ] بيانات صحيحة ← 200 مع رمز وصول، والتطبيق ينتقل إلى `/dashboard`.
- [ ] كلمة مرور خاطئة ← **422** مع `بيانات الدخول غير صحيحة` (وليس 401 — نموذج الدخول يعرض
      كائن `errors` الذي تُرجعه Laravel).
- [ ] بريد غير موجود ← **نفس** رسالة كلمة المرور الخاطئة (منعاً لاستكشاف الحسابات).
- [ ] `inactive.user@abusaleem.test` بكلمة المرور **الصحيحة** ← 422 مع
      `هذا الحساب غير مفعّل. يرجى مراجعة مدير النظام.` رسالة مستقلة: البيانات صحيحة لكن
      الحساب موقوف.
- [ ] 7 محاولات فاشلة خلال دقيقة ← **429**. الحد `throttle:6,1` هو 6 محاولات في الدقيقة لكل
      عنوان IP، وهو يحسب المحاولات الناجحة أيضاً — توقّع أن يعترضك هذا إذا جرّبت تسجيل دخول
      متتابع بسكربت.
- [ ] أثناء تسجيل الدخول، الانتقال إلى `/login` يُحوّل إلى `/dashboard` (`guestOnly`).
- [ ] بعد الخروج، الانتقال إلى `/reports` يُحوّل إلى `/login?redirect=/reports`، وتسجيل
      الدخول يُعيدك إلى `/reports`.
- [ ] أفسد الرمز في `localStorage` ثم أعد التحميل ← معترض 401 في axios يُخرجك بدل ترك واجهة
      معطوبة.
- [ ] تسجيل الخروج يُبطل الرمز: أعد إرسال الرمز القديم ← 401.

---

## 4. المسار الكامل — من المرحلة 1 إلى الأرشفة

الاختبار الجوهري. معاملة واحدة، ستة أشخاص مختلفين، إحدى عشرة حركة. الدور والحالة المتوقعة
مأخوذان مباشرةً من `WorkflowTransitionSeeder`.

**التحضير:** بحساب **R01**، أنشئ معاملة (القسم 8.1). النوع **تظلم GRIV** (مهلة 20 يوماً)، مع
**`decision_grade` = 15** — أي عند حد الدرجة (10) أو فوقه لكل الأنواع، فتسلك المسار الكامل
*عبر* الوزارة. سجّل الرقم المرجعي.

| # | المرحلة | الإجراء | سجّل الدخول بـ | المرحلة التالية | الحالة المتوقعة | توقيع؟ |
|---|---|---|---|---|---|---|
| 1 | 1 استلام المعاملة من البلدية | forward | **R02** | 2 | `in_review` | لا |
| 2 | 2 فحص استيفاء المتطلبات | approve | **R02** | 3 | `in_review` | **نعم** — مستوى 1 |
| 3 | 3 مراجعة المقرر | forward | **R02** | 4 | `in_review` | لا |
| 4 | 4 إبداء الملاحظات | forward | **R02** | 5 | `ready` | لا |
| 5 | 5 اعتماد الوزارة | forward | **R05** | 6 | `ready` | لا |
| 6 | 6 تحويل المعاملة للجنة | forward | **R05** | 7 | `in_meeting` | لا |
| 7 | 7 استلام المعاملة من اللجنة | approve | **R03** | 8 | `decided` | **نعم** — مستوى 2 |
| 8 | 8 الاعتماد حسب الصلاحيات | approve | **R05** | 9 | `approved` | **نعم** — مستوى 3 |
| 9 | 9 وزارة الحكم المحلي | approve | **R06** | 10 | `approved` | **نعم** — مستوى 4 |
| 10 | 10 اعتماد الجهة المختصة | approve | **R07** | 11 | `final_approved` | **نعم** — مستوى 5 |
| 11 | 11 الاعتماد النهائي والأرشفة | approve | **R07** | 11 (حلقة ذاتية) | `archived` | **نعم** — مستوى 6 |

- [ ] كل خطوة أعلاه تصل إلى المرحلة والحالة المتوقعتين.
- [ ] في كل خطوة، **الدور المذكور وحده** يرى زر الإجراء. سجّل الدخول بدور آخر في المرحلة
      نفسها وتأكد أن مساحة العمل لا تعرض أي إجراء.
- [ ] **R01 لا يستطيع تحريكها أبداً.** في المراحل الإحدى عشرة كلها يرى R01 شاشة التفاصيل
      (صلاحية العرض ممنوحة للجميع) دون أي أزرار سير عمل إطلاقاً.
- [ ] الخط الزمني للمراحل يزيد صفاً واحداً مع كل حركة، مع اسم المنفّذ ووقت التنفيذ.
- [ ] مسار الاعتمادات يعرض **ستة** اعتمادات موقّعة بالمستويات 1–6 مع ظهور صور التواقيع.
- [ ] بعد الخطوة 11 تصبح المعاملة `archived` و**نهائية** — لا يُعرض أي إجراء، ونداء
      `POST /api/transactions/{id}/transition` المباشر يُرجع 422.

### 4.1 تخطّي مستوى اعتماد مستحيل

- [ ] والمعاملة في المرحلة 2، سجّل الدخول بـ R05 ونفّذ
      `POST /api/approvals/admin-manager/{id}` ← 422 مع
      `لا توجد المعاملة في مستوى الاعتماد المطلوب.`
- [ ] والمعاملة في المرحلة 8، سجّل الدخول بـ R06 وجرّب قائمة اعتماد الوزارة ← الرسالة نفسها
      (لم تصل المعاملة إلى المرحلة 9 بعد).

---

## 5. مسار تجاوز الوزارة

الوجهة الوحيدة **غير** الظاهرة في خريطة الانتقالات المزروعة — يحسبها
`WorkflowService::destinationStageId()` وقت التنفيذ.

القاعدة: عند `approve` من المرحلة 8، إذا كانت `Transaction::requiresMinistryApproval()` تساوي
false تقفز المعاملة مباشرةً إلى المرحلة **10** متجاوزةً الوزارة. وهذه الدالة تكون false فقط
عندما يكون للنوع حدّ درجة وتكون `decision_grade` **أقل** منه. كل الأنواع المزروعة حدّها **10**.

أنشئ ثلاث معاملات وأوصل كلاً منها إلى المرحلة 8:

- [ ] `decision_grade = 5` (أقل من 10) ← يعتمدها R05 في المرحلة 8 ← تصل إلى **المرحلة 10
      اعتماد الجهة المختصة** متجاوزةً المرحلة 9. الحالة تبقى `approved`.
- [ ] `decision_grade = 10` (عند الحد تماماً) ← تذهب إلى **المرحلة 9** (الوزارة). الحدّ شامل.
- [ ] `decision_grade = 15` (فوق الحد) ← تذهب إلى **المرحلة 9**.
- [ ] في المعاملة المتجاوِزة، يحتفظ مسار الاعتمادات بالمستويات 1 و2 و3 ثم يقفز إلى **5** —
      غياب المستوى 4 (الوزارة) صحيح. المستويات ثوابت أعمال لا عدّاد تسلسلي، فالفجوة سليمة.
- [ ] R06 لا يرى المعاملة المتجاوِزة في قائمة اعتماد الوزارة إطلاقاً.

---

## 6. المسارات الاستثنائية

كل الانتقالات الاستثنائية الخمسة تشترط إدخال سبب (`requires_comment = true`).

| الإجراء | من ← إلى | الدور | الحالة الناتجة |
|---|---|---|---|
| نقص مستندات `return_missing_docs` | 2 ← 1 | R02 | `incomplete` |
| رفض المراجعة `reject_review` | 3 ← 2 | R02 | `rejected` |
| طلب تعديل `request_edit` | 4 ← 3 | R02 | `returned` |
| إلغاء `cancel` | حلقة ذاتية، أي مرحلة مفتوحة | حسب المرحلة\* | `cancelled` |
| تأجيل `defer` | 7 ← 7 | R03 | `deferred` |

\* دور الإلغاء حسب المرحلة: 1–4 لـ R02، 5–6 لـ R05، 7 لـ R03، 8 لـ R05، 9 لـ R06،
10–11 لـ R07.

- [ ] كل مسار من المسارات الراجعة الثلاثة ينقل المعاملة إلى المرحلة والحالة المتوقعتين
      ويُسجّل السبب في الخط الزمني.
- [ ] إرسال أي استثناء بسبب **فارغ أو مسافات فقط** ← 422. جرّب من نافذة الواجهة ومن نداء
      مباشر بـ `"comment": "   "`.
- [ ] الإلغاء من المرحلة 6 بحساب R05 ← الحالة `cancelled` والمرحلة تبقى **6**. الحلقة الذاتية
      مقصودة: تُسجّل أين توقّف العمل بدل التظاهر بأن الإلغاء تقدّمٌ نحو الاعتماد.
- [ ] المعاملة الملغاة نهائية — لا إجراءات مُتاحة، والواجهة البرمجية تُرجع 422، وتختفي من كل
      قوائم الاعتماد.
- [ ] الإلغاء من المرحلة 6 بحساب **R02** ← مرفوض (دور الإلغاء في المرحلة 6 هو R05).

---

## 7. المهل الزمنية وتصعيد التأخير

`due_date = submitted_at.startOfDay() + type.default_sla_days`. المهل المزروعة: إجازة LEAV 7،
علاوة ALLW 10، ترقية/انتداب/نقل 15، تظلم/إنهاء خدمة 20.

- [ ] أنشئ معاملة إجازة ← تاريخ الاستحقاق بعد 7 أيام. وأنشئ تظلماً ← بعد 20 يوماً.
- [ ] أرجِع تاريخ إحداها في قاعدة البيانات ليصبح `due_date` في الماضي، ثم شغّل
      `php artisan transactions:flag-overdue`.
- [ ] الأمر يطبع `Backfilled N deadline(s); flagged N overdue transaction(s).`
- [ ] `overdue_at` صار مضبوطاً، والتطبيق يُظهر تجاوز المهلة في شاشة التفاصيل.
- [ ] **شغّل الأمر مرة ثانية** — المعاملة نفسها لا يُعاد وسمها ولا يُرسَل إشعار ثانٍ. حارس
      `whereNull('overdue_at')` يجعل هذا يحدث مرة واحدة بالضبط.
- [ ] معاملة تستحق **اليوم** لا تُوسَم. التجاوز يبدأ في اليوم التالي فقط.
- [ ] المعاملة `cancelled` أو `archived` لا تُوسَم أبداً.
- [ ] إجراء **`deadline_expired`** (من R08 إلى إشراف الوزارة، مع سبب إلزامي) يظهر **فقط** على
      معاملة موسومة، ولـ R08 وحده. تحقّق من غيابه قبل تشغيل الأمر ومن ظهوره بعده.
- [ ] تنفيذه ينقل المعاملة إلى المرحلة 9 بحالة `in_review`.

---

## 8. الاستلام والمرفقات والملاحظات

### 8.1 الاستلام

`POST /api/transactions` خلف صلاحية `transaction_intake,add` (لـ R01–R06، **وليس R07**).

- [ ] R01 يفتح `/transactions/create`؛ و**R07 لا يستطيع** (تحويل + 403 على
      `GET /api/transactions/intake-options`).
- [ ] الحقول الإلزامية: `title` و`department_id` و`transaction_type_id`. نقص أيّها ← 422
      برسالة عربية.
- [ ] `decision_grade` **إلزامي** لكل نوع له حدّ درجة (والأنواع السبعة كلها لها حدّ)، ويجب أن
      يكون عدداً صحيحاً بين 1 و100. أهمله ← 422 مع
      `درجة القرار مطلوبة لهذا النوع من المعاملات.`
- [ ] قائمة الإدارات تعرض الإدارات **المفعّلة** و**ذات الرمز** فقط — عطّل إدارة من شاشة
      الإدارة وتأكد من اختفائها.
- [ ] صيغة الرقم المرجعي `YYYY-DEPT-000001`. أنشئ معاملتين في ENG وواحدة في FIN: ENG تصل إلى
      `-000002` بينما FIN تبدأ من `-000001` الخاص بها. العدّادات **لكل إدارة ولكل سنة**.
- [ ] المعاملة الجديدة بحالة `new` والمرحلة 1 و`submitted_at` مضبوط.
- [ ] الخط الزمني يحوي صفاً واحداً (الإجراء `intake`) قبل أن يلمسها أحد.

### 8.2 المرفقات

الحدود: `pdf,doc,docx,jpg,jpeg,png`، و20 ميغابايت للملف، و**10 ملفات كحد أقصى**.

- [ ] ملف PDF صالح يُرفع ويظهر في شاشة التفاصيل مع تسميته.
- [ ] ملف `.txt` أو `.exe` يُرفض — تحقّق من **المتصفح** (شاشة الاستلام تعكس الحدود محلياً)
      **ومن الواجهة البرمجية مباشرةً** بـ curl. يجب أن يرفض الخادم بمعزل عن المتصفح.
- [ ] ملف أكبر من 20 ميغابايت يُرفض.
- [ ] إرفاق 11 ملفاً عند الاستلام ← 422.
- [ ] المرفقات **غير** متاحة للعموم: انسخ المسار المخزَّن وحاول جلبه من جذر الويب. يجب ألّا
      يُستجاب له — الملفات على قرص خاص.
- [ ] R06 يستطيع عرض الملاحظات والمرفقات لكن **لا يستطيع الإضافة**
      (`notes_attachments,add` لـ R01–R05).

### 8.3 الملاحظات

- [ ] R01–R05 يستطيعون إضافة ملاحظة؛ R06 وR07 يقرآن ولا يضيفان.
- [ ] نص أطول من 5000 حرف ← 422.
- [ ] الملاحظة الداخلية (`is_internal`) مُميّزة بوضوح.

---

## 9. قوائم الاعتماد والتوقيع الإلكتروني

ست قوائم، كل واحدة مرتبطة بمرحلة واحدة ودور واحد.

| المسار | الشاشة | المرحلة | الدور |
|---|---|---|---|
| `/approvals/reviewer` | `reviewer_approval` | 2 | R02 |
| `/approvals/committee-head` | `committee_head_approval` | 7 | R03 |
| `/approvals/admin-manager` | `admin_manager_approval` | 8 | R05 |
| `/approvals/ministry` | `ministry_approval` | 9 | R06 |
| `/approvals/authority` | `authority_approval` | 10 | R07 |
| `/approvals/final` | `final_approval` | 11 | R07 |

- [ ] كل قائمة تعرض معاملات مرحلتها **فقط**. أوقف معاملات في مراحل متعددة وتأكد أن أي قائمة
      لا تعرض عمل غيرها.
- [ ] المعاملات `cancelled` و`archived` لا تظهر في **أي** قائمة.
- [ ] الاعتماد دون رسم توقيع ← مرفوض. التوقيع **إلزامي** في المستويات الستة كلها، بما فيها
      خطوة الأرشفة النهائية.
- [ ] التحقق من التوقيع: PNG فقط، 2 ميغابايت كحد أقصى، وأبعاد بين 2×2 و2400×1200 بكسل. أرسل
      ملف JPEG عبر curl ← 422.
- [ ] بعد الاعتماد يعرض المسار صورة التوقيع. الصورة تُحمَّل عبر نقطة نهاية تتطلب رمز وصول،
      لذا تأكد من ظهورها فعلياً لا من ظهور أيقونة صورة معطوبة.
- [ ] **تنظيف التوقيع عند الفشل:** اعتمد بملف PNG صالح لكن بحالة غير صالحة (مرحلة خاطئة أو
      دور لا تملكه) ← 422، و**لا يبقى ملف يتيم** في `storage/app/private/signatures`. عُدّ
      الملفات قبل وبعد.
- [ ] جلب توقيع اعتماد يخص معاملة أخرى بمعرّفه ← 404، لا صورة شخص آخر.

---

## 10. اللجان والاجتماعات والتصويت والقرارات

اللجان والاجتماعات كلتاهما تعملان بصلاحيات شاشة `meetings`: العرض للجميع، الإضافة لـ R03/R04،
**التعديل لـ R03 وحده**، والحذف لـ R08 وحده.

### 10.1 اللجان

- [ ] R03 يُنشئ لجنة (`name_ar` إلزامي).
- [ ] R04 يستطيع إنشاء لجنة (`add`) لكنه **لا يستطيع تعديلها** (`edit` لـ R03 فقط) ← 403.
- [ ] R03 يُضيف الأعضاء: `r03.head` و`r04.member1` و`r04.member2` و`r04.member3`، ويُعيّن
      `r03.head` رئيساً.
- [ ] إضافة المستخدم نفسه مرتين ← 422 مع `هذا المستخدم عضو بالفعل في اللجنة.`
- [ ] R08 لا يستطيع حذف لجنة عقدت اجتماعاً ← 422. مبدأ الحفظ لا المحو.

### 10.2 الاجتماعات وجدول الأعمال

- [ ] R03 يجدول اجتماعاً. **كل الأعضاء المفعّلين الأربعة يُدعَون تلقائياً** — راجع قائمة
      الحضور دون إضافة أحد يدوياً.
- [ ] أضف معاملة واقفة في المرحلة 7 إلى جدول الأعمال (استخدم مربع البحث — يطابق الرقم
      المرجعي أو العنوان).
- [ ] إضافة المعاملة نفسها مرتين مرفوضة.
- [ ] أعد ترتيب البنود بأزرار أعلى/أسفل؛ الترتيب يبقى بعد إعادة التحميل.
- [ ] احذف بنداً من جدول الأعمال.
- [ ] سجّل الحضور: اجعل `member1` و`member2` و`member3` **حاضرين**، واترك عضواً واحداً
      **غير حاضر** عمداً من أجل القسم 10.3.

### 10.3 التصويت — قواعد الأهلية

لدى `DecisionEligibility` ثلاث قواعد بالضبط، لكل منها رسالتها العربية. هذا أهم قسم يستحق
التأنّي.

- [ ] عضو حاضر يُصوّت `approve` ← 201.
- [ ] العضو يستطيع **تغيير** صوته قبل تسجيل القرار (صوّت ثانيةً، فيتحدّث الفرز بدل إنشاء صوت
      ثانٍ).
- [ ] **عضو لجنة لم يحضر** يُرفَض:
      `التصويت مقصور على الأعضاء المسجل حضورهم في هذا الاجتماع.` فالعضوية الدائمة لا تمنح
      صوتاً في جلسة تغيّبت عنها.
- [ ] **غير العضو** (مثل R05) يُرفَض: `التصويت مقصور على أعضاء هذه اللجنة.` لاحظ أن R05 لا
      يملك أصلاً `decisions,add`، فتأكد أن الـ403 يسبق؛ ثم اختبر بإخراج `multi.role` من
      اللجنة لعزل قاعدة العضوية وحدها.
- [ ] **قائمة الأصوات المعلّقة لا تكذب أبداً.** بكل حساب، افتح `/decisions` ← «بانتظار
      تصويتي». كل بند معروض يجب أن يكون قابلاً للتصويت، وأي بند سترفضه نقطة التصويت يجب ألّا
      يُعرض. هذا هو الثابت الوحيد الذي وُجد `DecisionEligibility` لحمايته — استعلام القائمة
      وحارس التصويت هما الشروط الثلاثة نفسها.

### 10.4 تسجيل القرار

`decisions,approve` — **لـ R03 وحده**.

- [ ] **لا أصوات** ← 422 مع `لا توجد أصوات مسجلة على هذا البند بعد.`
- [ ] **تعادل** (موافقة واحدة ورفض واحد) ← 422 مع
      `التصويت متعادل، لا يمكن حسم القرار تلقائياً.` ولا يُكتب أي صف قرار ولا تتحرك المعاملة.
- [ ] **موافقتان ورفض واحد** ← يُسجّلها R03 ← تنتقل المعاملة من **المرحلة 7 إلى 8** بحالة
      `decided`، ويُكتب صف اعتماد بالمستوى 2 مع التوقيع.
- [ ] أغلبية **تأجيل** ← المعاملة **تبقى في المرحلة 7** بحالة `deferred`، جاهزة لإدراجها في
      جدول أعمال اجتماع لاحق.
- [ ] أغلبية **رفض** ← تُترجَم إلى إجراء `cancel` ← الحالة `cancelled` والمرحلة تبقى 7.
- [ ] بعد وجود قرار يُغلق التصويت:
      `تم تسجيل قرار هذا البند بالفعل، لا يمكن التصويت بعد الآن.`
- [ ] البند يختفي من قائمة الأصوات المعلّقة لدى كل الأعضاء.
- [ ] الاجتماع الذي سُجّل فيه قرار **لا يمكن حذفه** ← 422.
- [ ] محاولة R04 تسجيل القرار ← 403 (`decisions,approve` لـ R03 وحده)، مع أن R04 صوّت.

### 10.5 سجل القرارات

- [ ] `/decisions` يعرض القرار المُسجَّل مع فرز الأصوات (2/1/0) واللجنة وتاريخ الاجتماع ومن
      أصدره.
- [ ] المرشّحات تُضيّق النتائج: النتيجة، اللجنة، المدى الزمني، والبحث بالرقم المرجعي أو
      العنوان.
- [ ] R08 يُصدّر السجل بصيغتَي xlsx وpdf؛ و**كل دور آخر يحصل على 403** على
      `/api/decisions/export` مع بقاء السجل ظاهراً له على الشاشة.
- [ ] التصدير **غير مُقسَّم لصفحات** — سجل يتجاوز صفحة واحدة يُصدَّر كاملاً.

---

## 11. الإشعارات

ستة أنواع أحداث. الافتراضات: داخل التطبيق مُفعّل للجميع؛ البريد مُفعّل للأحداث الأربعة التي
تطلب إجراءً؛ و**الرسائل النصية مُعطّلة في كل الأحداث** (لأنها مكلفة لكل رسالة، فهي اشتراك
اختياري).

| الحدث | داخل التطبيق | بريد | رسالة نصية | المستقبِلون |
|---|---|---|---|---|
| `transaction_created` | ✅ | ❌ | ❌ | فاعلو المرحلة التالية (لا المُنشئ) |
| `stage_changed` | ✅ | ❌ | ❌ | المُنشئ وحده |
| `action_required` | ✅ | ✅ | ❌ | فاعلو المرحلة التالية (لا المُنشئ ولا المنفّذ) |
| `transaction_overdue` | ✅ | ✅ | ❌ | المُنشئ + فاعلو المرحلة الحالية |
| `meeting_scheduled` | ✅ | ✅ | ❌ | صفوف الحضور المكتوبة للتو |
| `decision_recorded` | ✅ | ✅ | ❌ | المُنشئ + أعضاء اللجنة |

- [ ] R01 يُنشئ معاملة ← **R02** يستلم `action_required` (لأن R02 يملك الانتقال 1→2)، وR01
      لا يُخطَر بإجراء نفسه.
- [ ] R02 يُحوّلها ← **R01 (المُنشئ)** يستلم `stage_changed`، وفاعل المرحلة التالية يستلم
      `action_required`.
- [ ] شارة الجرس في الشريط العلوي تعرض عدد غير المقروء وتتحدّث تلقائياً خلال **60 ثانية**
      (استطلاع دوري لا websockets — لا تتوقعها فورية).
- [ ] فتح الجرس يعرض أحدث 5؛ و`/notifications` يعرض القائمة الكاملة مُقسَّمة لصفحات. التجاهل
      في أحدهما يُحدّث الآخر فوراً (مخزن مشترك).
- [ ] «تعليم الكل كمقروء» يُصفّر الشارة.
- [ ] **التفضيلات:** عطّل `email` لحدث `action_required`، ثم أطلقه، وتأكد من وصول الإشعار
      داخل التطبيق فقط (راجع `storage/logs/laravel.log` أو Mailpit للبريد).
- [ ] **إسكات حدث بالكامل** (تعطيل القنوات الثلاث) ← لا يُسلَّم شيء إطلاقاً.
- [ ] شاشة التفضيلات تعرض **الأحداث الستة كلها** دائماً، حتى ما لم تُغيّره قط.
- [ ] **الرسائل النصية:** فعّل `sms` لحدث ما. مع `SMS_DRIVER=log` يُكتب التسليم في السجل.
      امسح رقم هاتف الحساب ← تُسقَط قناة الرسائل عند حساب القنوات لا عند تنفيذ المهمة.
- [ ] **العزل بين المستخدمين:** كل نقاط النهاية محصورة بالمُنادي. خذ معرّف إشعار مستخدم آخر
      وحاول تعليمه مقروءاً ← يجب ألّا ينجح.
- [ ] حفظ تفضيلات لنوع حدث غير معروف ← 422.

---

## 12. سجل التدقيق

- [ ] الزرع لا يكتب **أي** صف تدقيق. بعد `migrate:fresh --seed` يكون العارض فارغاً.
- [ ] إنشاء معاملة وتعديلها والاعتماد والتصويت وتسجيل القرار — كلها تظهر.
- [ ] توسيع الصف يعرض فرق القيم القديمة/الجديدة **للحقول المتغيّرة فقط**.
- [ ] **الحفظ دون تغيير** (إرسال نموذج التعديل دون تعديل شيء) لا يُسجّل شيئاً.
- [ ] غيّر كلمة مرور مستخدم من شاشة المستخدمين ← صف التدقيق يعرض `********` لا التجزئة، على
      **جانبَي** الفرق.
- [ ] حذف سجل يُخزّن ما فُقد في `old_values`، والأثر يبقى بعد حذف السجل نفسه (أعمدة الربط
      المتعدّد بلا مفتاح أجنبي عمداً).
- [ ] المرشّحات تعمل: بالمستخدم، والإجراء، ونوع النموذج، ومعرّف السجل، والمدى الزمني.
- [ ] مرشّح النموذج يعرض سجلاً ثابتاً، لا النماذج التي صادف أن لها صفوفاً فقط.
- [ ] تمرير اسم صنف خام كمرشّح `model` **مرفوض** — الواجهة البرمجية لا تقبل اسم صنف من
      العميل أبداً.
- [ ] **لا توجد** نقطة كتابة. `POST`/`PUT`/`DELETE` على `/api/audit-logs` ← 405/404.
- [ ] R06 أو R07 يُصدّر السجل؛ وR01 يحصل على 403 مع قدرته على قراءته.

---

## 13. لوحة التحكم والتقارير

كلتاهما تقرآن من خدمة واحدة، فلا يمكن أن تتناقض بطاقة على الشاشة مع ملف مُصدَّر.

- [ ] لوحة التحكم تعرض الإجمالي والمعلّق والمنجز ونسبة الإنجاز وتجاوزات المهل ومتوسط زمن
      الدورة من بيانات حقيقية.
- [ ] الإجمالي = المنجز + المتوقّف + المعلّق. المعلّق يُحسب كالباقي، فالثلاثة تتطابق دائماً.
- [ ] `overdue` يعدّ العمل الموسوم **والمفتوح فقط** — أرشِف معاملة متأخرة وراقب انخفاض عدد
      التجاوزات.
- [ ] **إبطال التخزين المؤقت:** سجّل الإجمالي، أنشئ معاملة، أعد تحميل اللوحة. المعاملة
      الجديدة تظهر **فوراً** (مهلة 5 دقائق وحدها كانت ستبدو خللاً).
- [ ] أشرطة التوزيع حسب الحالة والمرحلة والإدارة (أعلى 10) ومنحنى 12 شهراً كلها تُرسَم دون
      مكتبة رسوم.
- [ ] الحالة التي لا معاملات لها تعرض 0 صراحةً بدل أن تختفي.
- [ ] مرشّحات التقارير (الإدارة، النوع، الحالة، المدى الزمني) تُضيّق الجدول والملخّص معاً،
      والملخّص يطابق الصفوف.
- [ ] التصدير بصيغة **xlsx** — يُفتح في Excel، واتجاه الورقة من اليمين لليسار عند اللغة
      العربية.
- [ ] التصدير بصيغة **pdf** — **النص العربي متّصل ومُشكَّل الحروف بشكل صحيح**، لا حروف
      منفصلة مرتّبة من اليسار لليمين. هذا هو السبب الوحيد لاعتماد mPDF؛ إذا فشل هذا الفحص
      فقد استبدل أحدهم مُولّد الـPDF.
- [ ] اسم الملف المُنزَّل يصل سليماً بما فيه الأحرف العربية.
- [ ] أطلق تصديراً لا تملك صلاحيته وتأكد من ظهور **رسالة خطأ مقروءة**، لا تنزيل يفشل صامتاً
      (أجسام أخطاء الـblob تُفكّ إلى JSON).

---

## 14. شاشات الإدارة (R08)

- [ ] **المستخدمون:** إنشاء مستخدم وإسناد الأدوار والتعديل وتفعيل/تعطيل الحساب والحذف. من
      تُعطّله لا يستطيع الدخول (القسم 3).
- [ ] **ملاحظة:** شاشة المستخدمين **لا تستطيع ضبط `phone`** — الحقل غير مقبول في طلبَي
      الإنشاء والتعديل مع أن العمود موجود. حسابات الاختبار تأخذه من البذرة، والمستخدمون
      يضبطون رقمهم من شاشة تفضيلات الإشعارات.
- [ ] **الإدارات:** أنشئ شجرة متداخلة؛ الإدارة التي لها أبناء أو مستخدمون **لا يمكن حذفها** ←
      عطّلها بدلاً من ذلك.
- [ ] **الأدوار والصلاحيات:** بدّل صلاحية واحفظ، ثم سجّل الدخول بذلك الدور وتأكد من سريان
      التغيير على **القائمة الجانبية والواجهة البرمجية معاً**. أعِدها بعد ذلك، أو أعد تشغيل
      `ScreenRolePermissionSeeder`.
- [ ] **الإعدادات:** إنشاء/تعديل/حذف زوج مفتاح-قيمة. القيم نصّية بحتة بالتصميم — لا تتوقع
      تحققاً من النوع.
- [ ] **القوالب:** أنشئ قالباً ثنائي اللغة؛ `code` و`name_ar` و`body_ar` إلزامية، والإنجليزية
      اختيارية.

---

## 15. النسخ الاحتياطي ودليل الاستخدام واتجاه الواجهة

### 15.1 النسخ الاحتياطي (لـ R08 وحده)

- [ ] **متطلّب مُسبق:** `mysqldump` ليس ضمن مسار النظام على هذا الجهاز — يجب أن يشير
      `BACKUP_MYSQLDUMP_PATH` في `.env` إليه (مثل `C:\xampp\mysql\bin\mysqldump.exe`، بين
      علامتَي اقتباس مفردة؛ فمكتبة dotenv ترفض الشرطات المائلة العكسية داخل الاقتباس المزدوج).
- [ ] أنشئ نسخة من `/backup`؛ يظهر الصف بالحجم ووقت الاكتمال.
- [ ] الأرشيف يحوي `database.sql` مع `files/attachments/` و`files/signatures/`.
- [ ] نزّل النسخة (`backup,export`)؛ ثم احذفها (`backup,delete`).
- [ ] **أعطِبها عمداً:** اضبط `BACKUP_MYSQLDUMP_PATH` على مسار خاطئ وأنشئ نسخة ← يظهر صف
      **`failed`** أحمر مع السبب، بدل خطأ 500 يضيع في السجل. أعِد المسار الصحيح بعدها.
- [ ] `php artisan backup:run` يطبع
      `Created <file> (<N> KB); pruned <N> expired snapshot(s).`
- [ ] `php artisan schedule:list` يُظهر `backup:run` عند 01:30 و`transactions:flag-overdue`
      عند 00:05.
- [ ] **لا يوجد زر استعادة، وهذا مقصود** — الشاشة تنصّ على ذلك. الاستعادة عملية تُنفَّذ على
      الخادم والنظام متوقف.

### 15.2 دليل الاستخدام

- [ ] R08 ينشر مقالاً (`code` و`title_ar` و`body_ar` إلزامية)؛ وكل دور آخر يقرؤه ويطبعه.
- [ ] **المسوّدة** (`is_active = false`) مخفية عن القرّاء وظاهرة لـ R08.
- [ ] تكرار `code` ← 422، لكن المقال يستطيع الاحتفاظ برمزه عند التعديل.
- [ ] **فحص XSS المخزَّن:** بحساب R08، الصق `<script>alert(1)</script>` و
      `<img src=x onerror=alert(1)>` في نص مقال. ثم سجّل الدخول بحساب R01 وتأكد أن كليهما
      يُعرَض **كنص حرفي**. النصوص تُعرَض كفقرات نصية لا عبر `v-html` إطلاقاً — فهذه الشاشة
      الوحيدة التي يدخلها كل دور، ولو عوملت كـHTML لصارت ثغرة XSS مُخزَّنة موجّهة إلى
      المؤسسة بأكملها.

### 15.3 الاتجاه واللغة والطباعة

- [ ] بدّل بين العربية والإنجليزية. `<html dir>` ينقلب بين `rtl` و`ltr`.
- [ ] القائمة الجانبية والجداول والنماذج والنوافذ كلها تنعكس بشكل صحيح. ابحث تحديداً عن أي
      عنصر مثبّت في الجهة الخاطئة — التخطيط يستخدم الخصائص المنطقية
      (`margin-inline-start`)، فأي `left`/`right` فيزيائي في مكوّن جديد سيظهر هنا.
- [ ] لا توجد مفاتيح غير مترجَمة (نصوص خام مثل `some.key.name`) في أي شاشة بأي من اللغتين.
- [ ] **جرّب معاينة الطباعة لكل شاشة.** كتلة `@media print` العامة تُحرّر `height:100vh`
      و`overflow:hidden` من `.shell`؛ وبدونها تحصل على الصفحة الأولى فقط. تأكد أن المحتوى
      الطويل يُقسَّم لصفحات فعلاً — تفاصيل المعاملة وسجل القرارات وسجل التدقيق أولى بالفحص.

---

## 16. اختبارات الانحدار والاعتماد النهائي

```bash
vendor/bin/phpunit         # المتوقع: 80 اختباراً ناجحاً
vendor/bin/pint --test     # الملفات المعدّلة نظيفة
cd frontend && npm run build
```

- [ ] المجموعة الكاملة ناجحة.
- [ ] `npm run build` ينجح.
- [ ] `php artisan migrate:status` — لا ترحيلات معلّقة.

### سجل الأخطاء

| # | القسم | الحساب | ما الذي حدث | المتوقع | الخطورة |
|---|---|---|---|---|---|
| 1 | | | | | |
| 2 | | | | | |
| 3 | | | | | |

---

## ملحق — البيانات المرجعية

**الأدوار:** R01 موظف · R02 المقرر · R03 رئيس اللجنة · R04 عضو اللجنة ·
R05 مدير إدارة الشؤون الإدارية · R06 وزارة الحكم المحلي · R07 المدير العام / العميد ·
R08 مدير النظام

**الإدارات:** ABS بلدية أبو سليم (الجذر) ← ADM، ENG، FIN، REP، CMT

**أنواع المعاملات** (حدّ درجة القرار = 10 للجميع):
PROM ترقية 15 يوماً · LEAV إجازة 7 · ALLW علاوة 10 · SECD انتداب 15 · GRIV تظلم 20 ·
TRNS نقل 15 · EOSV إنهاء خدمة 20

**الحالات:** جديد · قيد المراجعة · ناقص · جاهزة · في الاجتماع · قرار صادر · معتمدة ·
معتمدة نهائياً · **مؤرشفة** (نهائية) · مرجعة · مرفوضة · **ملغاة** (نهائية) · مؤجلة

**المراحل:** 1 استلام المعاملة من البلدية · 2 فحص استيفاء المتطلبات · 3 مراجعة المقرر ·
4 إبداء الملاحظات · 5 اعتماد الوزارة · 6 تحويل المعاملة للجنة · 7 استلام المعاملة من اللجنة ·
8 الاعتماد حسب الصلاحيات · 9 وزارة الحكم المحلي · 10 اعتماد الجهة المختصة ·
11 الاعتماد النهائي والأرشفة

**مصادر الحقيقة:** الأدوار والصلاحيات `database/seeders/ScreenRolePermissionSeeder.php` ·
سير العمل `database/seeders/WorkflowTransitionSeeder.php` + `app/Services/WorkflowService.php` ·
أهلية التصويت `app/Services/DecisionEligibility.php` ·
حسابات الاختبار `database/seeders/TestUserSeeder.php` · نطاق المراحل `STAGE_PLAN.md`
